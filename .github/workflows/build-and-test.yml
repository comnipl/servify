name: build-and-test

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        toolchain: [stable, beta, nightly]
        workspace: [servify, servify-client]

    steps:
      - uses: actions/checkout@v2
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v1
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.toolchain }}
      - name: Build
        run: cargo build --package ${{ matrix.workspace }} --verbose
      - name: Run tests
        run: cargo test --package ${{ matrix.workspace }} --verbose
